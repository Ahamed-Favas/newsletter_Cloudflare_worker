{
  "name": "my-newsletter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "vitest": "2.1.8",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "workers-ai-provider": "^0.0.10",
    "xmldom": "^0.6.0"
  }
}
